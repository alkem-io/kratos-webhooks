apiVersion: v1
kind: Service
metadata:
  name: alkemio-kratos-webhooks-service
spec:
  ports:
    - protocol: TCP
      name: kratos-webhooks
      port: 8080
  selector:
    app: alkemio-kratos-webhooks
